# Kioku updates

Signed Sparkle update feed and release artifacts for Kioku (formerly Scribe).

The Publish Sparkle feed workflow deploys `appcast.xml` to GitHub Pages on each feed change and verifies that the public feed matches the deployed commit. It can also be run manually from GitHub Actions. The public feed URL stays the same for existing app installations.
